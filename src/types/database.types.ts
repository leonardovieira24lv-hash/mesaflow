/**
 * PLACEHOLDER — este arquivo é gerado automaticamente pelo Supabase CLI e não
 * deve ser editado manualmente.
 *
 * Gerar com:
 *   npm run supabase:types
 * (requer SUPABASE_PROJECT_ID configurado e supabase login feito localmente)
 *
 * Até a primeira geração, o tipo `Database` fica como `any` para não travar o
 * build. Assim que o schema de `supabase/migrations` for aplicado ao projeto
 * remoto, rode o comando acima para substituir este placeholder.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
